{
  "name": "webpulse-ai",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm install --prefix backend && npm run build --prefix backend",
    "start": "node backend/dist/main.js"
  }
}
